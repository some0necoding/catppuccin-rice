(defwidget brightness []
  (box
    :class "brightness"
    :space-evenly false
    (label
      :class "brightness-icon"
      :text "${icon}"
    )
    (label 
      :text "${percent}%"
    )
  )
)

(deflisten icon    :initial "󰃠"   "scripts/brightness icon")
(deflisten percent :initial "100" "scripts/brightness percent")
