(defwidget battery []
  (box
    :class "battery"
     (circular-progress
      :value "${battery-percent}"
      :start-at 50
      :clockwise false
      :thickness 3
      (label
       :class "battery-icon"
       :text "󰁹"
      )
    )
  )
)

(deflisten battery-percent :initial "100" "scripts/battery percent")
